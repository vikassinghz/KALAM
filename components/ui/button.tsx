import React from 'react';

// This is a placeholder component to prevent build errors from an empty file.
// The application currently uses custom-styled buttons throughout the UI.
const Button = () => null;

export { Button };
